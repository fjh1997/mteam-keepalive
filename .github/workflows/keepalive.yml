name: M-Team keepalive

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - name: Run mtlogin
        run: |
          docker run --rm \
            -e USERNAME="${{ secrets.MT_USERNAME }}" \
            -e PASSWORD="${{ secrets.MT_PASSWORD }}" \
            -e TOTPSECRET="${{ secrets.MT_TOTPSECRET }}" \
            ghcr.io/${{ github.repository_owner }}/mtlogin:latest
